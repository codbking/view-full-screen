
import ComponentFullScreen from "./component-full-screen";

export default {
    install: (Vue) => {
        Vue.component("component-full-screen", ComponentFullScreen)
    }
}

export {ComponentFullScreen}
